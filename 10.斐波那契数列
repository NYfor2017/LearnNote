10.斐波那契数列
  题目：写一个函数，输入n，求斐波那契数列的第n项。
  要求：①对输入的数字进行合法性校验②保证时间复杂度在O(n)。
  实现：
  public class Solution{
     int Fibonacci(int n){
       int[] result = new int[2];
       result[0] = 0;
       result[1] = 1;
       if(n<0)
         throw new RuntimeException("输入不合法");
       else if(n<2){
         return result[n]; 
       }else{
         int num1 = 0;
         int num2 = 1;
         int sum = 0;
         for(int i=2;i<=n;i++){
           sum = num1+num2;
           num1 = num2;
           num2 = sum;
         }
         return sum;
       }
     }
  }
